# NovelAgent 项目规则

## 语言与写作

- 所有用户可见回复默认使用中文。
- 项目内用于 Agent 的 prompt 默认使用中文编写。
- 项目设计文档 design 默认使用中文编写。
- 项目实施计划 plan 默认使用中文编写。
- 仅当用户明确要求英文或其他语言时，才切换语言。

## 开发约定

- 新功能优先保持本地可测试，避免一开始绑定单一模型供应商。
- API Key、访问令牌等敏感信息不得写入源码或文档，必须通过环境变量读取。
- 长篇小说相关数据结构应优先保留可持久化、可审计的结构化字段。

